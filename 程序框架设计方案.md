# 程序框架设计方案

## 目标

本方案用于《占卜人生》首个可玩版本的程序框架整理，严格遵循以下开发原则：

- 类QFramework的小型框架
- 完全数据驱动，禁止把玩法内容写死在代码里
- 预留性能优化方案，包括对象池、异步加载、多线程等
- 不依赖正式美术素材，所有展示内容可用占位图、纯色块、文字描述完成
- 功能界面必须以节点方式搭建，方便在Godot编辑器中直接调整
- 不依赖纯代码动态生成整套UI，界面结构以场景节点为主
- 一个脚本不能过大，尤其是界面脚本必须拆分拆分再拆分

本框架适用于Godot 4.3。

---

# 一、整体设计思想

## 1.1 采用“小型QFramework风格”而不是重型框架

目标不是照搬QFramework全部内容，而是提取其最适合本项目的核心思想：

- 模块解耦
- 接口分层
- 数据驱动
- 命令式流程切分
- 系统间通过事件通讯
- UI、数据、业务逻辑分离

在本项目中，建议采用以下核心结构：

- `App`：应用入口
- `Model`：数据层
- `System`：系统层
- `Command`：流程命令层
- `Controller`：界面控制层
- `View`：节点界面层
- `Service`：资源、存档、线程、加载等服务层
- `Utility`：工具层

这套结构足够轻，适合单人或小团队维护，同时能避免脚本越来越乱。

---

# 二、目录结构建议

建议从一开始就把目录定死，避免后续大量返工。

```text
res://
├─ assets/
│  ├─ placeholder/
│  │  ├─ textures/
│  │  ├─ icons/
│  │  └─ fonts/
│  └─ audio/
├─ configs/
│  ├─ gameplay/
│  ├─ events/
│  ├─ ui/
│  └─ tables/
├─ data/
│  ├─ runtime/
│  └─ save/
├─ scenes/
│  ├─ app/
│  ├─ common/
│  ├─ pages/
│  ├─ widgets/
│  ├─ popups/
│  └─ world/
├─ scripts/
│  ├─ app/
│  ├─ core/
│  │  ├─ architecture/
│  │  ├─ event/
│  │  ├─ command/
│  │  ├─ pool/
│  │  ├─ async/
│  │  ├─ resource/
│  │  └─ utility/
│  ├─ model/
│  ├─ system/
│  ├─ command/
│  ├─ controller/
│  ├─ view/
│  ├─ service/
│  └─ debug/
└─ tests/
```

说明如下：

- `assets/placeholder/`：所有临时占位图、临时图标、简单色块纹理放这里
- `configs/`：所有假数据、表格数据、玩法配置都放这里
- `scenes/`：只放节点场景，不掺杂业务数据
- `scripts/`：所有脚本按职责分层
- `tests/`：后续可以补简单自测场景或数据校验脚本

---

# 三、核心分层设计

# 3.1 App层

## 作用

负责游戏启动、模块注册、初始化顺序、主场景切换。

## 推荐脚本

- `App.gd`
- `AppLauncher.gd`
- `GameRoot.gd`

## 职责

- 初始化基础服务
- 加载配置表
- 初始化运行时数据
- 启动主界面
- 管理全局生命周期

`App` 不处理具体玩法，不直接写页面逻辑。

---

# 3.2 Model层

## 作用

只负责保存数据状态，不处理界面，不处理复杂流程。

## 建议拆分

- `PlayerModel.gd`
- `DayCycleModel.gd`
- `WeatherModel.gd`
- `DebtModel.gd`
- `SuspicionModel.gd`
- `VillageRelationModel.gd`
- `InventoryModel.gd`
- `RunStateModel.gd`

## 设计原则

- Model只管数据
- 不允许直接操作节点
- 不允许直接依赖具体页面
- 数据变化通过事件或通知抛出

例如：

- `PlayerModel`：体力、健康、饱食、状态异常
- `InventoryModel`：粮食、铜钱、肉、药草、工具
- `DebtModel`：欠款数值、剩余天数、是否还清
- `SuspicionModel`：里正怀疑值、村民关注值

---

# 3.3 System层

## 作用

系统层负责规则运算，是玩法逻辑核心。

## 建议拆分

- `TimeSystem.gd`
- `WeatherSystem.gd`
- `FortuneSystem.gd`
- `ActionResolveSystem.gd`
- `EventTriggerSystem.gd`
- `DebtSystem.gd`
- `SuspicionSystem.gd`
- `VillageRelationSystem.gd`
- `ResourceSettlementSystem.gd`
- `SaveSystem.gd`

## 设计原则

- System只处理规则和算法
- 不直接写具体UI表现
- 优先读取配置数据，再结合Model运算
- 输出统一结果对象，交给Controller或Command继续处理

例如：

- `WeatherSystem` 根据天气配置表生成当天天气
- `FortuneSystem` 根据权重、天气、日期、地点生成三选一目标
- `ActionResolveSystem` 负责行动结果结算
- `SuspicionSystem` 根据玩家行为增减怀疑值

---

# 3.4 Command层

## 作用

负责流程编排，把多个System串起来。

如果说System是“规则模块”，Command就是“流程执行器”。

## 建议拆分

- `StartNewDayCommand.gd`
- `SelectFortuneCommand.gd`
- `ExecuteOutdoorActionCommand.gd`
- `ReturnVillageCommand.gd`
- `NightSettlementCommand.gd`
- `RepayDebtCommand.gd`
- `ApplyNpcEventCommand.gd`
- `EndDayCommand.gd`

## 设计原则

- 一个Command只做一件流程型工作
- 不把所有流程写进一个大脚本
- Command之间可以串联，但不要互相嵌套失控

例如：

`StartNewDayCommand` 可以做：

- 推进天数
- 随机天气
- 刷新每日占卜
- 触发开场事件
- 通知界面刷新

---

# 3.5 Controller层

## 作用

负责“界面和业务的桥梁”。

Controller不持有核心数据，只负责：

- 接收按钮输入
- 调用Command
- 订阅事件
- 推动View刷新

## 建议拆分

- `MainPageController.gd`
- `HudController.gd`
- `FortuneSelectController.gd`
- `ActionPageController.gd`
- `NightPanelController.gd`
- `NpcPopupController.gd`
- `SettlementPopupController.gd`

## 特别要求

界面脚本必须小。

禁止出现：

- 一个 `MainUI.gd` 里写完所有按钮逻辑、数据刷新、弹窗控制、状态计算

正确做法：

- 一个主页面只负责总挂载
- 每个局部组件单独一个 Controller + View 脚本
- 大面板拆成多个子面板

---

# 3.6 View层

## 作用

只负责节点显示和最少量交互转发。

## 建议拆分

- `HudView.gd`
- `ResourceBarView.gd`
- `DayInfoView.gd`
- `FortuneOptionItemView.gd`
- `FortunePanelView.gd`
- `ActionResultView.gd`
- `NightActionButtonView.gd`
- `NpcPopupView.gd`

## 原则

- View只做显示
- 不在View里直接写复杂业务逻辑
- 尽量避免View里写长流程判断
- View通过信号把点击事件抛给Controller

---

# 3.7 Service层

## 作用

提供公共服务能力，不绑定单一玩法。

## 建议拆分

- `ConfigService.gd`
- `ResourceService.gd`
- `AsyncLoadService.gd`
- `ThreadService.gd`
- `PoolService.gd`
- `SaveService.gd`
- `LogService.gd`

## 典型职责

- `ConfigService`：读取JSON、CFG、Resource配置
- `AsyncLoadService`：异步加载场景、贴图、音频
- `ThreadService`：把适合后台计算的工作丢到线程
- `PoolService`：管理可复用对象
- `SaveService`：负责存档序列化

---

# 四、数据驱动设计要求

## 4.1 原则

必须做到：

- 玩法内容不写死在代码里
- 配置与逻辑分离
- 规则代码只解释数据，不直接写具体内容
- 先写假数据也要按正式结构来

例如：

不能这样做：

- 在 `FortuneSystem.gd` 里直接写“第一项是小黑山野兔，第二项是河边野菜”

必须这样做：

- `configs/gameplay/fortune_options.json`
- `configs/events/outdoor_events.json`
- `configs/tables/weather_table.json`
- `configs/tables/item_table.json`

System 只负责读取这些配置并运行。

---

## 4.2 推荐数据文件分类

### 表配置

- `item_table.json`
- `weather_table.json`
- `location_table.json`
- `npc_table.json`
- `status_table.json`

### 玩法配置

- `fortune_pool.json`
- `day_cycle_config.json`
- `economy_config.json`
- `difficulty_config.json`

### 事件配置

- `outdoor_event_pool.json`
- `village_event_pool.json`
- `npc_event_pool.json`
- `ending_config.json`

### UI配置

- `ui_page_config.json`
- `popup_config.json`

---

## 4.3 假数据要求

即使现在只是原型，也必须用假数据跑通。

例如：

- 道具用简化字段
- 事件文本先写短描述
- 图标可用颜色块和文字占位
- 地点先只配3个
- 天气先只配4种

假数据的意义不是临时乱写，而是保证未来替换正式数据时不改架构。

---

# 五、节点式界面设计原则

## 5.1 禁止纯代码生成整套主界面

你的要求是正确的。

本项目所有主要功能界面都应该在Godot编辑器中做成 `.tscn` 节点界面，方便：

- 直接拖拽布局
- 可视化调整锚点
- 分块替换占位资源
- 非程序人员也能看懂结构

## 5.2 推荐界面分层方式

### 页面层

- `MainPage.tscn`
- `ActionPage.tscn`
- `NightPage.tscn`
- `SettlementPopup.tscn`

### 面板层

- `HudPanel.tscn`
- `DayInfoPanel.tscn`
- `ResourcePanel.tscn`
- `SuspicionPanel.tscn`
- `FortuneSelectPanel.tscn`

### 小组件层

- `LabelValueItem.tscn`
- `FortuneOptionItem.tscn`
- `ResourceItem.tscn`
- `NpcDialogOptionItem.tscn`

## 5.3 为什么要这样拆

因为这样可以保证：

- 每个场景职责明确
- 每个脚本很小
- 每个界面改动影响范围小
- 后面接美术方便替换

---

# 六、脚本拆分规则

这是本项目最重要的工程纪律之一。

## 6.1 拆分底线

### UI脚本拆分原则

- 一个页面一个总Controller
- 一个局部区域一个View脚本
- 一个复杂弹窗一个独立Controller
- 一个列表项一个独立Item脚本

### 流程脚本拆分原则

- 一天开始一个Command
- 一次外出行动一个Command
- 一次夜间结算一个Command
- 一个系统只做一个领域

## 6.2 禁止出现的情况

- 一个脚本里同时写数据、表现、流程、加载、存档
- 一个UI脚本超过几百行后继续塞逻辑
- 一个God类负责所有功能

## 6.3 推荐约束

建议执行以下规则：

- View脚本尽量控制在100到200行内
- Controller脚本尽量控制在150到250行内
- System脚本尽量只负责单领域规则
- 超出后优先拆子类，不要硬撑

---

# 七、性能优化方案

虽然当前是原型，但性能基础要提前规划。

## 7.1 对象池

### 适用对象

- 列表项
- 弹窗选项按钮
- 事件卡片
- 飘字
- 小图标提示
- 可重复刷新的UI单元

### 建议模块

- `PoolService.gd`
- `NodePool.gd`
- `PooledItem.gd`

### 原则

- 高频创建销毁的节点一律走池
- 尤其是滚动列表和事件选项列表
- 不要每次刷新都 `instantiate` + `queue_free`

---

## 7.2 异步加载

### 适用内容

- 页面切换
- 弹窗资源
- 大图
- 音频
- 后续复杂场景

### 建议模块

- `AsyncLoadService.gd`
- `ScenePreloadTask.gd`
- `ResourceWarmupCommand.gd`

### 目标

- 主流程页面切换不明显卡顿
- 能预加载下一阶段可能要用的资源
- 为后续扩展保留加载界面能力

Godot 4 可使用线程化资源加载接口或后台异步任务封装。

---

## 7.3 多线程

### 适合放到线程中的工作

- 大量配置解析
- 存档读写
- 批量事件筛选
- 较复杂的权重随机计算
- 大表预处理

### 不适合直接放线程中的工作

- 直接操作场景树
- 直接改UI节点
- 直接增删Node

### 建议模块

- `ThreadService.gd`
- `BackgroundJob.gd`
- `MainThreadDispatcher.gd`

### 原则

后台线程只做数据计算，计算完后把结果送回主线程应用。

---

## 7.4 配置缓存

所有配置表加载后应缓存在内存中，不要频繁重复读磁盘。

建议：

- `ConfigService` 启动时加载基础表
- 高频配置转为字典索引
- 提供统一访问接口

---

## 7.5 分帧处理

对于长列表刷新、大批量UI更新、批量数据初始化等操作，应预留分帧处理。

例如：

- 事件列表逐帧填充
- 预热资源逐步加载
- 大量Item复用分批刷新

---

# 八、占位美术策略

## 8.1 当前阶段不等美术

你要求功能先行，这非常合理。

因此所有依赖美术的部分都采用占位方案：

- 纯色矩形背景
- 简单边框
- 文字标签
- 简单图标
- 临时绘制的小纹理

## 8.2 推荐占位方式

### 方案1：纯色块 + Label

例如：

- 天气图标：蓝色框写“雨”
- 肉类图标：棕色框写“肉”
- 粮食图标：黄色框写“粮”

### 方案2：简单自绘纹理

可做一些极简占位图：

- 方形包裹图标
- 卷轴图标
- 山体图标
- 人物头像轮廓

### 方案3：纯文本描述

事件卡片、选项卡、奖励栏都可以先用文本描述。

例如：

- “小黑山找野兔，低风险，收益：肉x1-2”

## 8.3 原则

先保证功能逻辑跑通，再替换资源，不反向影响程序架构。

---

# 九、推荐场景结构

## 9.1 应用入口场景

```text
AppRoot.tscn
└─ GameRoot
   ├─ UILayer
   ├─ PopupLayer
   ├─ LoadingLayer
   └─ DebugLayer
```

## 9.2 主页面场景

```text
MainPage.tscn
└─ MainPage
   ├─ HudPanel
   ├─ DayInfoPanel
   ├─ ResourcePanel
   ├─ SuspicionPanel
   ├─ FortunePanelAnchor
   ├─ ActionPanelAnchor
   └─ NightPanelAnchor
```

## 9.3 占卜面板场景

```text
FortuneSelectPanel.tscn
└─ FortuneSelectPanel
   ├─ TitleLabel
   ├─ OptionList
   │  ├─ FortuneOptionItem
   │  ├─ FortuneOptionItem
   │  └─ FortuneOptionItem
   └─ ConfirmButton
```

## 9.4 资源栏场景

```text
ResourcePanel.tscn
└─ ResourcePanel
   ├─ FoodItem
   ├─ MoneyItem
   ├─ HerbItem
   └─ MeatItem
```

这类结构不要靠代码动态拼整页，而是在编辑器中直接搭节点。

---

# 十、推荐核心类清单

下面是首版建议的类清单，已经尽量按小而散拆开。

## 10.1 App与Core

- `App.gd`
- `AppLauncher.gd`
- `GameRoot.gd`
- `Architecture.gd`
- `EventBus.gd`
- `CommandDispatcher.gd`
- `ServiceLocator.gd`

## 10.2 Model

- `PlayerModel.gd`
- `InventoryModel.gd`
- `DayCycleModel.gd`
- `WeatherModel.gd`
- `DebtModel.gd`
- `SuspicionModel.gd`
- `VillageRelationModel.gd`
- `FortuneSelectionModel.gd`

## 10.3 System

- `TimeSystem.gd`
- `WeatherSystem.gd`
- `FortuneSystem.gd`
- `ActionResolveSystem.gd`
- `SuspicionSystem.gd`
- `VillageRelationSystem.gd`
- `DebtSystem.gd`
- `SettlementSystem.gd`
- `EventTriggerSystem.gd`

## 10.4 Command

- `StartGameCommand.gd`
- `StartNewDayCommand.gd`
- `GenerateFortuneOptionsCommand.gd`
- `ConfirmFortuneSelectionCommand.gd`
- `ExecuteActionCommand.gd`
- `ProcessNightActionCommand.gd`
- `RepayDebtCommand.gd`
- `EndDayCommand.gd`

## 10.5 Service

- `ConfigService.gd`
- `ResourceService.gd`
- `AsyncLoadService.gd`
- `ThreadService.gd`
- `PoolService.gd`
- `SaveService.gd`
- `DebugDrawService.gd`

## 10.6 UI Controller

- `MainPageController.gd`
- `HudController.gd`
- `DayInfoController.gd`
- `ResourcePanelController.gd`
- `SuspicionPanelController.gd`
- `FortuneSelectController.gd`
- `ActionPanelController.gd`
- `NightPanelController.gd`
- `PopupController.gd`

## 10.7 View

- `HudView.gd`
- `DayInfoView.gd`
- `ResourcePanelView.gd`
- `SuspicionPanelView.gd`
- `FortuneSelectView.gd`
- `FortuneOptionItemView.gd`
- `ActionResultView.gd`
- `NightPanelView.gd`
- `NpcPopupView.gd`

---

# 十一、数据流建议

推荐采用以下数据流：

`View输入 -> Controller -> Command -> System -> Model更新 -> EventBus通知 -> Controller/View刷新`

优点：

- 流向清晰
- 易查问题
- 易替换数据源
- 易做调试面板

禁止反向乱穿：

- View直接改Model
- System直接操作UI
- Controller直接手写全部结算逻辑

---

# 十二、适合本项目的开发顺序

## 第一阶段：框架底座

先做：

- `App`
- `Architecture`
- `EventBus`
- `CommandDispatcher`
- `ConfigService`
- `基础Model`

目标：项目具备最小框架骨架。

## 第二阶段：主循环跑通

再做：

- `StartNewDayCommand`
- `WeatherSystem`
- `FortuneSystem`
- `ActionResolveSystem`
- `EndDayCommand`

目标：能从第1天走到第30天。

## 第三阶段：节点界面搭建

再做：

- `MainPage`
- `HudPanel`
- `FortuneSelectPanel`
- `ActionPanel`
- `NightPanel`

目标：主要功能都能通过节点界面操作。

## 第四阶段：数据驱动强化

补上：

- 道具表
- 事件池
- 地点表
- 天气表
- NPC事件表

目标：玩法内容彻底从代码中剥离出来。

## 第五阶段：性能预埋

补上：

- 池化列表项
- 资源异步加载
- 配置缓存
- 后台数据预处理

目标：后面扩内容时不返工。

---

# 十三、工程纪律

为了保证后续不崩，建议从一开始就遵守以下纪律：

## 13.1 任何玩法内容先加配置，再写逻辑适配

不要先写死，再想着以后抽配置。

## 13.2 任何页面先拆节点结构，再写Controller

不要先写一个大UI脚本顶着。

## 13.3 任何高频复用节点先考虑池化

尤其列表、选项卡、事件卡片。

## 13.4 任何耗时操作默认思考是否异步或后台处理

包括：

- 配置初始化
- 大量事件筛选
- 存档读取
- 资源预载

## 13.5 任何脚本变大先拆，不要忍

尤其UI脚本和流程脚本。

---

# 十四、结论

本项目推荐采用“轻量QFramework式 + 数据驱动 + 节点化界面 + 强拆分 + 性能预埋”的程序框架。

重点不是追求炫技，而是保证：

- 结构稳定
- 内容可扩展
- 数据可替换
- 页面可视化调整
- 单脚本不过大
- 后续加功能不推翻重来

这份框架尤其适合当前阶段：

- 美术未到位
- 玩法需要快速验证
- 需要先做Demo
- 未来还要持续扩展系统

---

# 十五、建议下一步

如果下一步开始正式搭工程，推荐直接按以下顺序执行：

- 建目录
- 建核心框架脚本
- 建主入口场景
- 建主页面节点结构
- 建配置假数据文件
- 先跑通一天流程
- 再接30天循环
- 再补性能层

如果继续推进，下一步最适合整理的是：

- Godot项目目录落地方案
- 第一批场景和脚本空壳清单
- 配置表字段设计
- 首批假数据样例
